#include <math.h>

int find_matrix_kernel(int matrix[], int kernel[], int matrix_row, int matrix_col, int kernel_row, int kernel_col){
    //сворачивающаяся матрица теряет элемент или не теряет
    return 1;
}