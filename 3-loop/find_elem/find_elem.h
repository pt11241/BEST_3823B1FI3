#include <math.h>

int find_elem(int left, int right, int number, int max_iter){
    int i = 0;
    // int j = 0;
    // int m = (left + right)/2;
    // while (j < max_iter){
    //     j++;
    //     if (m == number){
    //         break;
    // }   else{
    //     if (number < m) right = m - 1;
    //     else left = m + 1;
    // }
    // return j;
    return i; //НЕ МЕНЯТЬ!
}